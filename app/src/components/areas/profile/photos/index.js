import React, { useEffect, useState } from "react";
import { connect } from "react-redux";

import "./PhotosTab.scss";
import { Container, Col, Row } from "react-bootstrap";

const PhotosTab = ({}) => {
  return <></>;
};

export default connect((state) => {
  return {};
}, {})(PhotosTab);
